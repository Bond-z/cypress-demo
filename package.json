{
  "name": "cypress-demo",
  "version": "1.0.0",
  "description": "*** Virtual env\r 'pip install virtualenv'\r run: 'virtualenv env'\r activate: 'env\\scripts\\activate'\r deactivate: 'env\\scripts\\deactivate'",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:smoke": "cypress run --spec 'cypress/e2e/spec.cy.js' --env TAGS=@smoke",
    "cy:regression": "cypress run --spec 'cypress/e2e/spec.cy.js' --env TAGS=@test",
    "cypress:open": "cypress open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cypress/grep": "^4.1.0",
    "@testing-library/cypress": "^10.0.2",
    "cypress": "^13.13.3",
    "cypress-log-to-output": "^1.1.2"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  }
}
